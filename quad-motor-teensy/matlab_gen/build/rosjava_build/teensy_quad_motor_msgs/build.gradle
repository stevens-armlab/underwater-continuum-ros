/*
 * Copyright 2015-2018 The MathWorks, Inc.
 */

try {
    project.catkin.tree.generateMessageArtifact(project, 'teensy_quad_motor_msgs')
} catch (NullPointerException e) {
    println("Couldn't find teensy_quad_motor_msgs on the ROS_PACKAGE_PATH")
}

task bugfixtask << {
    mkdir sourceSets.main.output.classesDir
}
jar.dependsOn(bugfixtask)
